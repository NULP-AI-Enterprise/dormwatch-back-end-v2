apiVersion: v1
kind: Service
metadata:
  name: dormwatch
  namespace: dormwatch
spec:
  type: ClusterIP
  selector:
    app: dormwatch
  ports:
    - name: http
      port: 8000
      targetPort: http
